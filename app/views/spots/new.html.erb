<h1>New spot</h1>

<% form_for(@spot) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :occupied %><br />
    <%= f.check_box :occupied %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', spots_path %>